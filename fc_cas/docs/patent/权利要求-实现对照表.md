# 权利要求—实现对照表

> 代码依据为 FC-CAS 当前 CPU 实现。状态“部分”表示存在对应接口或离线/模拟验证，但不等同于完整在线推理实现；状态“待实现”不得作为已完成实验或产品能力主张。

| 权利要求 | 对应模块 / 函数 | 当前证据 | 状态 |
|---|---|---|---|
| 1 | `segmenter/json_fc.py::classify_prefix`；`strategy/table.py::StrategyTable.lookup`；`runtime/dspark_bridge.py::cas_proposal_hparams` | 按区段选择 block size、阈值和模式；CPU mock 验证策略路由 | 部分 |
| 2 | `segmenter/json_fc.py::classify_prefix`、`_arguments_cursor`、`_scan_arguments_object`；`types.py::Region` | 增量前缀识别骨架、枚举和自由参数区段 | 已实现 |
| 3 | `schema/guard.py::enum_allowed`；`segmenter/json_fc.py::classify_prefix` | 从 `tools.function.parameters.properties.<field>.enum` 提取允许枚举值并输出至 `SegmentInfo.allowed_strings` | 部分 |
| 4 | `schema/guard.py::next_template_prefix`；`strategy/table.py::DEFAULT_TABLE` | 为已打开的 arguments 对象建议必需字段模板前缀；骨架区段采用 `template` 模式 | 部分 |
| 5 | `types.py::Strategy.confidence_threshold`；`strategy/table.py::DEFAULT_TABLE`；`runtime/dspark_bridge.py::cas_proposal_hparams` | 区段阈值已配置并向运行时桥接暴露；置信度头及 token 级截断尚未接入 | 部分 |
| 6 | `eval/offline_stats.py::serialize_assistant`；`segmenter/json_fc.py::classify_prefix`；`schema/guard.py::enum_allowed` | 处理 OpenAI 风格 `tool_calls` 与 JSON 字符串 `function.arguments` | 已实现 |
| 7 | `eval/offline_stats.py::compute_offline_stats`；`experiments/scripts/run_offline_stats.py` | test 离线区段统计已完成；领域草稿训练和真实模型分区域接受长度/加速比评测因 T8、在线权重待办而未完成 | 部分 |
| 8 | — | 无推理负载感知的验证长度动态调整实现 | 待实现 |
| 9 | `segmenter/`、`strategy/`、`schema/`、`runtime/` 子包 | 对应装置逻辑模块的代码划分；完整目标模型验证器尚未接入 | 部分 |
| 10 | `src/fc_cas/` Python 包 | 方法可由通用处理器执行；未提供车机部署工程 | 部分 |
| 11 | `src/fc_cas/` 源码包与测试 | 程序载体代码已具备；介质权利要求为法律表达，不单独对应运行时代码 | 部分 |

## 评测证据

- `experiments/results/offline_stats_test.json`：991 条 assistant 序列、109,625 个序列化字符；模板/枚举可覆盖字符 94,004（85.75%）。
- `experiments/results/compare_mock_test.json`：20 条 test 样本的 CPU mock 路由检查；不产生 \(\tau\)、模型质量或 Speedup 结论。
